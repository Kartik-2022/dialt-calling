
// src/data/sampleData.js
// This file will eventually hold data fetched from your live API.
// For now, it's empty to simulate no data until API integration.

// Define the CallRecord interface (or type) for data structure
// In a real project, this might be in a separate types/interfaces file
// export interface CallRecord { // This would be in a .ts or .d.ts file for TypeScript
//   id: string;
//   createdAt: string;
//   candidateName: string;
//   contactDetails: string;
//   jobFunction: string;
//   user: string;
//   tags: string[];
// }

export const sampleData = []; // Now an empty array
